/**
 * Rocker Chat Component
 * 
 * Connects to global Rocker context - single persistent AI across all pages.
 */

import { RockerChatUI } from './RockerChatUI';

export function RockerChat() {
  return <RockerChatUI />;
}
