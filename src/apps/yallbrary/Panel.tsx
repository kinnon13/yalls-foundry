import type { AppUnitProps } from '@/apps/types';

export default function YallbraryPanel({}: AppUnitProps) {
  return (
    <div data-testid="panel-yallbrary" aria-label="Yallbrary Panel">
      —
    </div>
  );
}
