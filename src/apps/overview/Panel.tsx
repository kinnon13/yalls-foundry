import type { AppUnitProps } from '@/apps/types';

export default function OverviewPanel() {
  return (
    <div data-testid="panel-overview" className="p-4 text-sm text-muted-foreground">
      Quick metrics
    </div>
  );
}
