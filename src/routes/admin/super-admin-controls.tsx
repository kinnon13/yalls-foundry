/**
 * Super Admin Controls Route
 * CRITICAL: Only accessible to super_admin role
 */

import { SuperAdminControls } from '@/components/admin/SuperAdminControls';

export default function SuperAdminControlsPage() {
  return <SuperAdminControls />;
}
